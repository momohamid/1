name: U RDP Test

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  test:
    runs-on: windows-latest

    steps:
      - name: Test workflow
        shell: pwsh
        run: |
          Write-Host "================================"
          Write-Host "U RDP WORKFLOW CREATED"
          Write-Host "================================"
          Write-Host "Windows runner is working."
